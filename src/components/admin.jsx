import React from 'react';
import Typography from '@mui/material/Typography';

export default function Admin({ classes, boat }) {
    return (<Typography>editors get special treatment</Typography>);
}